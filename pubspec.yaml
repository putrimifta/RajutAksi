name: rajutaksi_app
description: "RajutAksi - Platform Relawan, Organisasi, dan Sponsor untuk SDG 17"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # Supabase
  supabase_flutter: ^2.5.6

  # State management ringan
  provider: ^6.1.2

  # UI & util
  google_fonts: ^6.2.1
  intl: ^0.19.0
  image_picker: ^1.1.2
  cached_network_image: ^3.3.1
  timeago: ^3.6.1
  flutter_svg: ^2.0.10+1
  uuid: ^4.4.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true